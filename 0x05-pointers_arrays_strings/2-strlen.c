#include "main.h"
#include <string.h>

/**
* _strlen - returns the length of a string
*
* @s: string passed
*
* Return: returns length of string passed
*/

int _strlen(char *s)
{
	int len;

	len  = strlen(s);
	return (len);
}
